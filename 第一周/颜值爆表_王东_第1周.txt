极客时间
	gRPC 效率高的原因 
		面向tcp，传输层
		传输协议高效

	传统企业的中台：联通，共享，融合
		中台=业务中台+数据中台
		业务中台分为通用能力中台+核心能力中台
	
	dubbo未来规划
		社区投入
		功能增强
			核心功能继续增强
			springboot继续升级
		生态建设
		dubbo3.0
			和内部hsf整合
		service mesh
		服务治理：
			不重试 不要默认设置重试
			设超时 设置超时时间
			可隔离 隔离：实例隔离，线程隔离
			可治理 支持限流，支持全链路跟踪，支持服务降级，熔断等 
			可验证 支持单机灰度发布，支持全链路的灰度
			
			
	 jvm典型案例分析
		死锁 就stack -m(慎用，到导致线程挂起)
		finalreference堆积  jmap -histo
		堆外内存泄漏 物理内存已经用完了，但是jvm内存还有，这个jvm启动要配置最大堆外内存
		YGC拉长 jmap -histo:live pid
			
		    
		 
		
		
		